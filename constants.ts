
export const API_URL = "https://api.gettnship.com/v2/tracking/ups";

// Thresholds
export const DAYS_FOR_QUEUE = 4; // If order is newer than this, queue it
export const DAYS_FOR_DELIVERY_WARNING = 7; // If delivery is sooner than this, warn
